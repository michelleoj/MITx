/*
Sends the player's guess to designated puzzle.
@return result the server's message
@throws IOException 
*/
*/~
/*
Sends the player's guess to designated puzzle.
@return server message
@throws IOException 
*/
*/~
/*
Sends the player's guess to designated puzzle.
@return result
@throws IOException 
*/
*/~
public String makeGuess() throws IOException {

    	URL csail = new URL("http://courses.csail.mit.edu/6.005/jotto.py?puzzle=" + puzzleNum + "&guess=" + guess);
    	URLConnection yc = csail.openConnection();
        BufferedReader in = new BufferedReader(new InputStreamReader(yc.getInputStream()));
        String inputLine;
        String result = "";
        while ((inputLine = in.readLine()) != null) {
        	result = inputLine;
        }
        in.close();
    	
    	return result;
	
    }